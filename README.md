# fph-sagemaker

Small project scaffold for training and deploying models with AWS SageMaker.

## What’s included
- Basic project structure
- Placeholder for notebooks and scripts

## How to use
1. Initialize git (if not already):
   ```sh
   git init
   git add .
   git commit -m "Initial commit"
   ```
2. Create a GitHub repo and push (example using gh CLI):
   ```sh
   gh repo create <owner>/fph-sagemaker --public --source=. --remote=origin --push
   ```

Replace `<owner>` with your GitHub username or organization.
